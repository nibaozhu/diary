 
转发服务器（i1）

* 启动转发服务器的方法,默认绑定 0.0.0.0:12340
	$ ./i1

* 客户端如何注册：
当客户端连接到转发服务器,服务器会为其分配唯一的8位的十六进制“客户编号”

* 消息协议：
最多8位十六进制字符 + 分隔符 + 发送者客户编号 + 分隔符 + 接收者客户编号 + 分隔符 + 消息体 + 分隔符
例如：你的客户端编号是 3934614d，对方的客户端编号是 0197cd96，要发生的消息是 "hello"，则消息体长度为 00000005
	分割显示如下 5|3934614d|0197cd96|hello|

* 如何发送心跳：
将接收者客户编号填为发送者客户编号，例如你的客户端编号是 3934614d
	分割显示如下 0|3934614d|3934614d||

